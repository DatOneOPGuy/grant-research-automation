import NotRewired from './NotRewired'

// Awaiting migration to the v5 API (the v1 preset endpoint is retired).
export default function BestProspects() {
  return <NotRewired name="Best Prospects" />
}
