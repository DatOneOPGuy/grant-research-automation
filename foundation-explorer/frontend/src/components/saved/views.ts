/** Sentinel for the "everything, regardless of folder" view on the Saved page.
 *  Lives here rather than in the page so the folder rail can import it without
 *  a circular page -> component -> page dependency. */
export const ALL = '__all__'
